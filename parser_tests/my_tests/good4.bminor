// declarations with initializations
i: integer  = 5;
b: boolean  = true;
s: string   = "string";
c: char     = '!';
v: void     = "actual value doesn't matter";
a: array ['c'] array ["string"] char = { {1, 'c', "str"}, {c, f, e}, {c + 5, d || x} };

f: function void(x: integer, a: boolean) = {
    return x;
    print a, b, c, 1, 3, a[14 ^ 5 % 6];
    x: integer;
    c: char;
    x = !4;

    {
        // here's a block
        x = 6;
        d = "15" + "tring";
    }
}

